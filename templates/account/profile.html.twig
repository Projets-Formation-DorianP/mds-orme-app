{% extends 'base.html.twig' %}

{% block title %}Modifier son profil{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <p>Edit</p>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit">Moifier mes mofidications</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}