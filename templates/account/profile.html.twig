{% extends 'layouts/base.html.twig' %}

{% block title %}ORME - Modifier le profil{% endblock %}

{% block body_class %}profile{% endblock %}
{% block body %}
    <div class="container">
        <div class="d-flex flex-column">
            <div class="menu">
                <ul class="nav nav-tabs">
                    <li class="nav-item">
                        <a class="nav-link active" href="/account/profile">Informations du profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/account/update-password">Changer de mot de passe</a>
                    </li>
                </ul>
            </div>
            <div class="content mt-2">
                <h4>Éditer le profil</h4>

                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary" type="submit">Enregister</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}