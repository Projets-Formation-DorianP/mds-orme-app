<nav class="navbar navbar-expand-lg navbar-light bg-light shadow">
    <a class="navbar-brand" href="#">
        <img src="{{ asset('build/logo.png') }}" height="48" alt="">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarPrimary" aria-controls="navbarPrimary" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarPrimary">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="#">Lien 1 <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Lien 2</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Lien 3</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Lien 4</a>
            </li>
        </ul>
        <ul class="navbar-nav flex-row ml-md-auto d-none d-md-flex">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-primary" href="#" role="button" id="dropdownProfile" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <img class="avatar mr-2" 
                            {% if app.user.picture %}
                                src="{{ app.user.picture }}" 
                            {% else %}
                                src="{{ asset('build/user.png') }}" 
                            {% endif %}
                        alt="">
                    {{ app.user.firstname }} {{ app.user.lastname }}</a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownProfile">
                        <a class="dropdown-item" href="/account/profile"><i class="fa fa-user-circle"></i> Mon compte</a>
                        <a class="dropdown-item" href="/logout"><i class="fa fa-sign-out-alt"></i> Se déconnecter</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="btn btn-outline-primary mr-2" href="/register" role="button"><i class="fa fa-edit"></i> Inscription</a>
                    <a class="btn btn-primary" href="/login" role="button"><i class="fa fa-sign-in-alt"></i> Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>